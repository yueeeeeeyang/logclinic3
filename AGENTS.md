# AGENTS.md

本文件定义本仓库内所有自动化编码代理、协作者和后续维护者必须遵守的工程规则。规则适用于整个仓库，除非更深层目录中存在额外的 `AGENTS.md` 并明确覆盖部分规则。

## 项目背景

本项目是一个日志查看客户端程序，目标运行平台为 macOS 和 Windows，技术栈为 Rust + GPUI。

核心目标：

- 提供稳定、可维护、跨平台一致的日志查看体验。
- 优先保证日志读取、解析、过滤、搜索、渲染和错误处理的正确性。
- 在 UI、文件系统、编码格式、权限、路径、性能和大文件处理等方面显式处理平台差异。

## 最高优先级强制约束

以下规则具有最高优先级，任何实现、重构、测试、文档和代码评审都必须遵守。

### 1. 需求不完整时必须先提问

当需求存在以下任一情况时，必须先向用户提出具体问题，不能直接自行假设并开始实现：

- 需求存在歧义，例如同一个操作可能有多种合理解释。
- 缺少业务规则，例如日志格式、过滤规则、排序规则、时间范围规则、错误显示规则未定义。
- 缺少权限说明，例如是否允许读取任意本地路径、是否允许记住最近打开文件、是否允许访问系统日志目录。
- 缺少数据边界，例如最大日志文件大小、单行最大长度、支持的编码、支持的换行格式、是否支持实时追踪追加内容。
- 缺少验收标准，例如什么行为算完成、需要覆盖哪些平台、需要哪些测试或可视化验证。

提问要求：

- 问题必须具体、可回答，并直接关联到实现决策。
- 如果有多个可选方案，应说明每个方案对行为、复杂度或风险的影响。
- 在用户回答前，不得提交会固化业务规则的实现。
- 对于低风险的纯机械修改，可以继续执行，但不得改变有歧义的业务行为。

### 2. 所有代码必须添加完整中文注释

所有文件、方法和逻辑代码必须添加完整、详细、有效的中文注释。

注释必须解释：

- 业务意图：这段代码服务于哪个用户场景或产品行为。
- 关键约束：跨平台差异、性能限制、权限限制、线程限制、UI 状态约束等。
- 边界条件：空输入、大文件、非法路径、编码异常、权限不足、日志行过长、时间格式异常等。
- 非显而易见的实现原因：为什么选择当前算法、数据结构、错误处理方式或 UI 状态流转。

注释禁止：

- 只重复代码字面含义，例如“设置变量”“调用函数”“返回结果”。
- 用英文替代中文说明，除非是 API 名称、协议名、类型名、错误码或第三方术语。
- 写过时、含糊或与代码行为不一致的说明。

实现代码时应优先做到：

- 每个源码文件顶部说明该文件在日志查看客户端中的职责、跨平台注意事项和主要边界。
- 每个公开类型、公开方法、复杂私有方法都应有中文文档注释。
- 复杂逻辑块前应有中文行注释，说明业务意图和边界处理。
- 测试代码也必须有中文注释，说明测试覆盖的业务规则和失败风险。

## Rust 代码规范

- 优先使用稳定 Rust，不引入 nightly 特性，除非用户明确同意。
- 遵循 `rustfmt` 默认格式。
- 遵循 `clippy` 建议，但不得为了消除告警牺牲可读性或业务正确性。
- 错误处理必须显式，禁止在可恢复路径中使用 `unwrap()`、`expect()` 或静默忽略错误。
- 与文件、路径、编码、时间、线程、异步任务相关的逻辑必须说明跨平台影响。
- 所有平台相关代码必须通过清晰的模块、条件编译或适配层隔离，避免在业务逻辑中散落平台判断。
- 对外暴露的数据结构应优先保持简单、可序列化、可测试。

## GPUI 实现规范

- UI 代码必须清楚区分状态、渲染和副作用。
- UI 状态字段必须用中文注释说明来源、生命周期、更新时机和空值含义。
- 长耗时任务，例如读取大文件、解析日志、全文搜索、实时追踪文件追加，不能阻塞 UI 线程。
- UI 中展示的错误必须可理解，不能只显示底层系统错误字符串。
- macOS 和 Windows 的字体、路径展示、快捷键、文件选择器行为差异必须显式考虑。
- 交互行为必须有明确验收标准；若需求未说明，应先提问。

## 日志查看业务约束

涉及日志文件时，必须优先确认或显式处理以下问题：

- 支持的文件来源：普通文件、目录、系统日志、远程日志、拖拽文件、最近打开记录。
- 支持的编码：UTF-8、UTF-8 BOM、GBK、Shift-JIS、Latin-1 或其他编码。
- 支持的换行格式：LF、CRLF、CR。
- 最大文件大小和内存策略：一次性加载、分块读取、索引、虚拟列表或流式处理。
- 单行最大长度和超长行展示策略。
- 时间戳识别规则、时区规则和排序规则。
- 搜索、过滤、高亮、跳转、大小写敏感、正则表达式、多条件组合等规则。
- 文件变更监听和实时追踪追加内容的行为。
- 权限不足、文件不存在、文件被占用、文件被截断、编码失败时的用户可见行为。

如果上述规则未在任务中明确给出，并且会影响实现行为，必须先提出问题。

## 测试与验证

- 修改业务逻辑时必须补充或更新测试。
- 修改 UI 行为时应尽可能补充可运行的状态测试、组件测试或手动验收说明。
- 跨平台相关逻辑必须至少说明 macOS 和 Windows 的差异风险。
- 文件读取、编码、路径、权限、超大文件、空文件、异常日志行等边界必须优先测试。
- 若当前环境无法运行某类测试，最终说明中必须明确哪些测试未运行以及原因。

## 依赖管理

- 新增依赖前必须确认其必要性、维护状态、许可证和跨平台支持。
- 不得为了简单功能引入过重依赖。
- 涉及日志解析、编码检测、文件监听、虚拟列表、异步运行时等关键依赖时，应说明选择原因和替代方案。

## 提交流程

- 修改前先阅读相关代码和现有模式。
- 不得重写无关文件或进行无关格式化。
- 不得回滚用户已有改动，除非用户明确要求。
- 提交前应运行与修改范围匹配的格式化、检查和测试命令。
- 最终回复必须简洁说明修改内容、验证结果和已知风险。

