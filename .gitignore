# 本地 Cargo 源配置只服务当前开发环境，不应进入代码仓库。
# 业务原因：
# - 用户要求当前项目默认使用清华源，但该镜像选择属于本机开发配置，不应强加给其它协作者或 CI。
# - 真实配置保存在 `.cargo/config.toml`，这里仅声明忽略规则，防止后续初始化 Git 后误提交。
.cargo/config.toml

# Rust 构建产物体积大且可由 Cargo 重新生成，不应进入代码仓库。
# 业务原因：
# - 提交构建产物会显著放大仓库体积，并让不同平台构建结果互相污染。
# - macOS 和 Windows 后续都需要本地构建，因此仓库只保存源码、锁文件和配置。
target/

# 打包脚本生成的 DMG、EXE 和临时应用目录体积较大，应作为发布产物而不是源码提交。
# 业务原因：
# - macOS 与 Windows 的安装包由当前平台、签名证书和目标工具链决定，提交到仓库会造成跨平台污染。
# - 发布时应从脚本重新生成 dist 目录，源码仓库只保存可复现的打包脚本。
dist/

# macOS Finder 自动生成的目录元数据不包含业务信息，不应进入代码仓库。
# 业务原因：
# - 不同开发者的 Finder 视图状态不同，提交后会产生无意义变更。
# - 当前项目需要保持源码提交干净，避免本地系统文件干扰代码评审。
.DS_Store
